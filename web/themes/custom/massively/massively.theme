<?php

/**
 * @file
 * Functions to support theming in the massively theme.
 */

use Drupal\Core\Render\Markup;

/**
 * Implements hook_page_attachments_alter().
 *
 * @param array<string, mixed> $page
 *   The page attachments render array, altered by reference.
 */
function massively_page_attachments_alter(array &$page): void {
  $viewport = [
    '#type' => 'html_tag',
    '#tag' => 'meta',
  ];
  $page['#attached']['html_head'][] = [$viewport, 'viewport'];
  $fallback = '<link href="/themes/custom/massively/assets/css/noscript.css" rel="stylesheet" />';
  $noscript = [
    '#tag' => 'noscript',
    '#value' => Markup::create($fallback),
  ];
  $page['#attached']['html_head'][] = [$noscript, 'fallback'];
}

/**
 * Implements hook_preprocess_page() for page.html.twig.
 *
 * @param array<string, mixed> $variables
 *   Template variables, altered by reference.
 */
function massively_preprocess_page(array &$variables): void {
  $form = \Drupal::formBuilder()
    ->getForm('Drupal\doesdesign_tools\Form\ContactForm');
  $variables['contact_form'] = $form;
}

/**
 * Implements hook_theme_suggestions_page_alter().
 *
 * @param array<int, string> $suggestions
 *   Existing theme suggestions, altered by reference.
 * @param array<string, mixed> $variables
 *   Template variables for the base theme hook.
 */
function massively_theme_suggestions_page_alter(array &$suggestions, array $variables): void {

}

/**
 * Implements hook_preprocess_menu_local_task().
 *
 * @param array<string, mixed> $variables
 *   Template variables, altered by reference.
 */
function massively_preprocess_menu_local_task(array &$variables): void {
  $variables['element']['#link']['url']->setOption('attributes', ['class' => 'button']);
  if ($variables['element']['#active']) {
    $variables['element']['#link']['url']->setOption('attributes', ['class' => 'button primary']);
  }
}

/**
 * Adds tag to all pages.
 *
 * @param array<string, mixed> $page
 *   The page attachments render array, altered by reference.
 */
function massively_page_attachments(array &$page): void {
  $fallback = '<link href="fallback.css" rel="stylesheet" />';
  $noscript = [
    '#tag' => 'noscript',
    '#value' => Markup::create($fallback),
  ];
  $page['#attached']['html_head'][] = [$noscript, 'mymodule'];
}
