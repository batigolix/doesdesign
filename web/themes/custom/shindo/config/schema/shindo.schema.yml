# AI generated
shindo.settings:
  type: theme_settings
  label: 'Shindo theme settings'
  mapping:
    body_font:
      type: string
      label: 'Body font'
      constraints:
        Choice:
          choices:
            - source-sans-pro
            - lora
            - roboto
            - system
    primary_color:
      type: color_hex
      label: 'Hoofdkleur'
    background_pattern:
      type: string
      label: 'Background pattern'
      constraints:
        Choice:
          choices:
            - bg
            - none
            - stripes
            - grid
    background_color:
      type: color_hex
      label: 'Background color'
