{#
/**
 * @file
 * Unformatted rows for the homepage_banner view (bead doesdesign-xgx).
 *
 * Strips the default views-view-unformatted wrapper <div> so the banner
 * markup mirrors Dopetrope's minimal <section id="banner"> structure.
 *
 * AI generated.
 */
#}
{% for row in rows %}
  {{ row.content }}
{% endfor %}
