{#
/**
 * @file
 * Theme override for the main navigation menu block.
 *
 * AI generated. Strips Drupal's default <div> wrapper + label and outputs
 * the menu directly inside <nav id="nav"> so Dopetrope's #nav CSS applies.
 */
#}
<nav id="nav">
  {{ content }}
</nav>
