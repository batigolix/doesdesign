{#
/**
 * @file
 * Theme override for the news → block_footer_dates views block.
 *
 * AI generated. Emits the view's content directly (no <div class="block">
 * wrapper) so the surrounding <section><header>…</header> in
 * page.html.twig plus the view's own <ul class="dates"> template form the
 * exact Dopetrope footer pattern.
 */
#}
{{ title_prefix }}
{{ title_suffix }}
{{ content }}
