{#
/**
 * @file
 * Theme override for a block placed in the footer_col_1 region.
 *
 * AI generated (bead doesdesign-g7v). Renders content bare — the
 * outer <section><header><h2>Recent nieuws</h2></header> wrapper is
 * hardcoded in page.html.twig. This template strips the block's own
 * wrapper so the news view list appears directly under the heading.
 */
#}
{{ title_prefix }}{{ title_suffix }}
{{ content }}
