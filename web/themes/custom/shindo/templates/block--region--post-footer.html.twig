{#
/**
 * @file
 * Theme override for a block placed in the post_footer region.
 *
 * AI generated (bead doesdesign-g7v). Renders content bare — the
 * outer <div id="copyright"> wrapper is hardcoded in page.html.twig.
 * Colofon block renders its <p> directly inside that div.
 */
#}
{{ title_prefix }}{{ title_suffix }}
{{ content }}
